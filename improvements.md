# Verbesserungen

- Die meisten Startkapitel müssen stark überarbeitet werden.
- Die Algorithmen müssen so geändert werden, das wir das ganze über Zustände
  und Breakpoints auswerten. Das heisst auch das die Videos überarbeitet werden
  müssen.
- Verschlüsselung wird noch Verbessert für Gym, dann auch für FMS übernommen
  und angepasst.
- Einstieg wird die Hardware noch mehr angeschaut. Wir brauchen eine Grafik mit
  den notwendigen Komponenten von einem Computer, und eine gute Visualisierung
  wie man von der reale Welt (also Text) zu den binären Daten auf dem Computer
  kommt.
